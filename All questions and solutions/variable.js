// 1. Create a variable.js file and declare variables and assign string, boolean, undefined and null data types,
//    Display all the value with there type.


let names = "Ankit";
console.log(names + ' and the datatype is ' + typeof(names));

let boolVar = true;
console.log(boolVar + ' and the datatype is ' + typeof(boolVar));

let unDefined;
console.log(unDefined + ' and the Datatype is ' + typeof(unDefined));

let person = null;
console.log(person + ' and the datatype is ' + typeof(person));

